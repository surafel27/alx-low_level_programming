#include <stdio.h>

/**
 * add - the function adds the two number
 * @a: integer assigned as parameter
 * @b: integer assigned as parameter
 *
 * Return: sum
 */
int add(int a, int b)
{
	return (a + b);
}
