#include "main.h"
#include <string.h>

/**
 * _strcpy - function copies sring
 * @dest: character pointer
 * @src: character pointer
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
