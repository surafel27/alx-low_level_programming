#include "main.h"
#include <string.h>

/**
 * _strlen - function to count the length of string
 * @s: character pointer
 * Return: length
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
